name: Docker Image build

on:
  push:
    branches: [ main ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build the Docker image
      run: |
        docker run -t -v `pwd`:/app/test khawla-k-banydomi/schedulerapi sh -c "ls -la && pwd && whoami && gulp install && gulp build && gulp test"
